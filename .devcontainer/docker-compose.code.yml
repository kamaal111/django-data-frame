services:
  code:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: django-data-frame-code
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
